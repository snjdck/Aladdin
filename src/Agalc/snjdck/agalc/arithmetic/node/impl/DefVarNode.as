package snjdck.agalc.arithmetic.node.impl
{
	import snjdck.agalc.arithmetic.node.Node;
	import snjdck.agalc.arithmetic.node.NodeType;
	import snjdck.arithmetic.IScriptContext;
	
	internal class DefVarNode extends Node
	{
		public function DefVarNode(type:NodeType, value:String=null)
		{
			super(type, value);
		}
		
		override public function calculate(context:IScriptContext):*
		{
			var value:Object = rightChild && rightChild.calculate(context);
			context.newKey(leftChild.value, value);
		}
	}
}